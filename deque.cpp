#include<bits/stdc++.h>
using namespace std;

int main(){
    // deque<int> dq;//defining deque
    // dq.push_back(1);
    // dq.push_back(2);
    // dq.push_back(3);

    
    // cout<<dq.front()<<" "<<dq.back()<<endl;

    // dq.pop_front();
    // dq.pop_back();

    //This will print deque in ascending order
    //as we are deleting elements from front and  
    //display front element
    // while(!dq.empty()){
    //     cout<<dq.front()<<" ";
    //     dq.pop_front();
    // }
    // cout<<endl;

    //This will print deque in descending order
    //as we are deleting elements from back and  
    //display back element

    
    // while(!dq.empty()){
    //     cout<<dq.back()<<" ";
    //     dq.pop_back();
    // }
    // cout<<endl;

    return 0;
}